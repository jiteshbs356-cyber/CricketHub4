services:
  - type: web
    name: cricket-universe
    runtime: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: CRICAPI_KEY
        sync: false
      - key: NODE_ENV
        value: production
